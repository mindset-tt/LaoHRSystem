# 09 — Leave Analytics

## API
`GET /api/analytics/leave?range=&from=&to=` (scoped).

## Metrics
Approved, Pending, Rejected leave requests; leave days by type; usage trend.

## No statutory carry-over
Only configured balances/usage reported. Lao statutory minimum entitlement is NOT reinterpreted.
